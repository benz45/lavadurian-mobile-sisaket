import 'package:flutter/material.dart';

class AllStatusOrderScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('All Status Order Screen'),
      ),
      body: Text('Coming soon...'),
    );
  }
}
